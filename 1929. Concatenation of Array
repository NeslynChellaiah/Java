class Solution {
    public int[] getConcatenation(int[] nums) {
        int[] ans = new int[2 * nums.length];
        int len = nums.length;
        for (int i = 0; i < len; i++) {
            ans[i] = nums[i];
            ans[len + i] = nums[i];
        }
        return ans;
    }
}
